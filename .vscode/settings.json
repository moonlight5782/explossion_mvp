{
    "gitdoc.enabled": true,
    "emeraldwalk.runonsave": {
        "commands": [
            {
                "match": ".*",
                "cmd": "${workspaceFolder}/smart_auto_sync.bat"
            }
        ]
    }
}